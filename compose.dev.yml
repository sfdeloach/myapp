services:
  database:
    image: postgres:18-alpine
    container_name: database-dev
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - pgdata-dev:/var/lib/postgresql
    networks:
      - app-network

  server:
    image: sfdeloach/myapp-server-dev:latest
    build:
      context: ./server
      dockerfile: Dockerfile.dev
    container_name: server-dev
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_PORT=${POSTGRES_PORT}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_SSLMODE=${POSTGRES_SSLMODE}
      - AUTO_SEED=true # Set to false to skip seeding
    volumes:
      - ./server:/server # Mount source code for live reloading
      - go-modules:/go/pkg/mod # Cache Go modules
    expose:
      - "3000"
    depends_on:
      - database
    networks:
      - app-network

  admin:
    image: sfdeloach/myapp-admin-dev:latest
    build:
      context: ./admin
      dockerfile: Dockerfile.dev
    container_name: admin-dev
    restart: unless-stopped
    expose:
      - "8080"
    depends_on:
      - database
    networks:
      - app-network

  web:
    image: sfdeloach/myapp-web-dev:latest
    build:
      context: ./web
      dockerfile: Dockerfile.dev
    container_name: web-dev
    restart: unless-stopped
    ports:
      - "8080:80" # Different port for dev to avoid conflicts
    depends_on:
      - server
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  pgdata-dev:
  go-modules:
